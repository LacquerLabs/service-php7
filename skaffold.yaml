apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/lacquerlabs/service-php7:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/lacquerlabs/service-php7:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: service-php7
        chartPath: charts/service-php7
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/lacquerlabs/service-php7"
          image.tag: "{{.TAG}}"
